import 'package:fl_clash/provider_client/provider_client_app.dart';
import 'package:flutter/material.dart';

class V2etShellPage extends StatelessWidget {
  const V2etShellPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const V2ETAuthGate();
  }
}
